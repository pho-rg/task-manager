import { TaskDateFormatPipe } from './task-date-format.pipe';

describe('TaskDateFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new TaskDateFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
